// Placeholder export for @frogtato/shared.
//
// Protocol message types (shared/src/messages.ts) and game constants
// (shared/src/constants.ts) are added in task T2. This file only proves the
// workspace/project-reference wiring between shared, server and client.
export const SHARED_PLACEHOLDER = true;
